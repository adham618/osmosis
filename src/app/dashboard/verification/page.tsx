import type { Metadata } from 'next'

import Vertification from '@/components/register'

export const metadata: Metadata = {
  title: '',
  description: ''
}

export default function VertificationPage() {
  return <Vertification />
}
